# Server-side fetches (SSR) call the API directly — not through /api rewrites.
# Browser client code uses /api/* which Next rewrites to this URL.
API_URL="http://localhost:3001"

# Production (Vercel): see .env.production.example — set in Vercel dashboard, not here.
