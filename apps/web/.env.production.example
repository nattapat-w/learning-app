# Production web env — set on VERCEL (not committed with real values).
# Copy to: Vercel → Project → Settings → Environment Variables
#
# CRITICAL: check Production + Preview (NOT Development only).
# Your live *.vercel.app URL uses the Production environment.
#
# For local dev: apps/web/.env.local (from .env.example).

API_URL=https://learning-app-oiw6.onrender.com
