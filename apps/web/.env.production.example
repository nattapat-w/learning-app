# Production web env — set on VERCEL (not committed with real values).
# Copy to: Vercel → Project → Settings → Environment Variables
# Apply to Production + Preview (unless you have a separate staging API).
#
# For local dev: apps/web/.env.local (from .env.example).
# next.config.ts reads API_URL at build time — must exist on Vercel before deploy.

API_URL=https://learning-app-api.onrender.com
